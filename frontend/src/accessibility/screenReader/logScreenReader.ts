export function logScreenReader(value: any, obj?: any) {
  // if (obj) {
  //   // Note: no stringify - (for events might have circular structure)
  //   console.log(`[SCREEN-READER->${value}]:`, obj);
  //   return;
  // }
  // console.log(`[SCREEN-READER->${value}]:`);
}
